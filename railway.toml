# https://docs.railway.com/reference/config-as-code
# API service: Root directory = repo root (empty), Dockerfile = backend.Dockerfile
# (build context must include video-production/ for Remotion). See backend/RAILWAY.md.
[build]
builder = "DOCKERFILE"
dockerfilePath = "backend.Dockerfile"
