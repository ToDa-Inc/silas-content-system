.git
**/.git
**/node_modules
**/.next
.venv
backend/.venv
**/__pycache__
**/*.pyc
data
.env
**/.env.local

# Do NOT list `content-machine/` here: the repo-root Next.js Dockerfile must COPY it in.
# (**/node_modules already trims heavy dirs.) These shrink context for builds that do not need them:
docs/
.claude/
