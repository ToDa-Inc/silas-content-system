# Environment for this repo is centralized at the monorepo root.
#
#   Template:  ../.env.example
#   Your file: ../.env   (copy from template once, then fill in)
#
# Optional overrides (later wins): backend/.env, then config/.env
#
# From this folder:
#   cp ../.env.example ../.env
