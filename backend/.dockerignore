# Used when Docker build context is `backend/` only. The production API image is built from the
# repo root with `backend.Dockerfile` (see backend/RAILWAY.md); that build uses the root `.dockerignore`.
.venv
**/__pycache__
**/*.pyc
.pytest_cache
